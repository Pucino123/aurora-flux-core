
## Verification: All Features from the List

After thorough codebase inspection, here is the status of every item from the user's requirement list:

---

### 1. Brand Rename — "Flux" → "Dashiii"

**MOSTLY DONE — with 3 remaining "Flux" references in user-facing UI:**

✅ `SEO.tsx` — `SITE_NAME = "Dashiii"`, `DEFAULT_TITLE = "Dashiii | Your AI Workspace"` ✓  
✅ `LandingPage.tsx` — Navbar, hero copy, footer all say "Dashiii" ✓  
✅ `OnboardingFlow.tsx` — Line 126: "DASHIII — WORKSPACE OS" ✓  
✅ `i18n.ts` — Both `da` and `en` locales use `"app.name": "Dashiii"` ✓  
✅ `NotificationBell.tsx` — `NOTIF_KEY = "dashiii-notifications-v2"` ✓  
✅ `CRMContext.tsx` — `LS_KEY = "dashiii_crm_deals"` ✓  
✅ `index.html` — Title and meta already say "Dashiii" ✓  
✅ `FluxSidebar.tsx` — Displays `{t("app.name")}` = "Dashiii" ✓  
✅ `BillingView.tsx` — SEO title says "Dashiii" ✓  

❌ **3 remaining user-facing "Flux" references:**
- `src/pages/Auth.tsx` line 136: `"Start your productivity journey with Flux"` / `"Sign in to your Flux workspace"` — still says "Flux"
- `src/components/council/CouncilBoardroom.tsx` lines 1266, 1458, 1490 — exported PDF/text says "Generated by Flux Boardroom" and "Powered by Flux Boardroom"
- `supabase/functions/notion-export/index.ts` line 120 — Notion export says "Flux Boardroom"

The internal hook/context names (`useFlux`, `FluxContext`, `FluxSidebar`, `FluxProvider`) are code-level names, not user-facing UI, so they don't need renaming.

---

### 2. Mobile Responsiveness
✅ `MobileNav.tsx` — Full bottom tab bar + slide-up drawer with hamburger menu, all hidden on `md:` screens  
✅ `LandingPage.tsx` — All responsive prefixes (`text-[2.6rem] sm:text-6xl md:text-[5rem]`, etc.)  
✅ `FluxSidebar.tsx` — Hidden on mobile, `MobileNav` used instead  
✅ `use-mobile.tsx` — `useIsMobile` hook exists  
✅ Touch targets — most interactive elements have `min-w-[44px] min-h-[44px]` in `MobileNav`

---

### 3. Dark/Light Theme
✅ `ThemeProvider` wraps the whole app with `defaultTheme="dark"` and keyboard shortcut `Ctrl+Shift+D`  
✅ Theme toggle button in `FluxSidebar` and `LandingPage` navbar  
✅ Council components use conditional styling for both themes

---

### 4. Sparks Pricing Overhaul
✅ `BillingView.tsx` — 3-tier `SPARK_PACKS` with bonus Sparks: 50/$5, 120/$10 (+20 bonus), 300/$20 (+100 bonus)  
✅ `SPARK_ACTIONS` table: 1 Spark = quick AI, 2 = Live Council, 3 = Deep Doc Analysis, 5 = Full Council  
✅ `SparksCheckoutModal.tsx` — Package selector with "Popular" badge  
✅ `FluxSidebar.tsx` — Sparks balance pill always visible in sidebar  
✅ `MonetizationContext.tsx` — `consumeSparks()` with float animations

---

### 5. CRM Dual Interface
✅ `CRMContext.tsx` — Global state (localStorage + Supabase sync)  
✅ `CRMPage.tsx` — Full-page CRM with pipeline stages, add/edit/delete deals  
✅ `CRMDashboardWidget.tsx` — Compact dashboard widget  
✅ `FluxSidebar.tsx` — CRM nav link present (line 164)  
✅ Both components use `useCRM()` from same context = synced

---

### 6. Inbox AI Text Area Removal
✅ `InboxView.tsx` — Uses `SegmentedControl` for Chat/Mail/Tasks with split-pane layout  
✅ No floating AI text area visible in current InboxView code

---

### 7. Calendar Feature
✅ `FullCalendarView.tsx` — Full calendar with Month/Week/Agenda views, multi-view navigation  
✅ `CalendarView.tsx` — Additional calendar component using CRM + Flux data  
✅ Event creation with CRM contact linking

---

### 8. Tasks System
✅ `AITaskManager.tsx` — Full tasks page (List + Kanban views)  
✅ `TaskManagerWidget.tsx` — Dashboard widget synced via `useFlux()` global context  
✅ Tasks have Priority, due date, and filters

---

### 9. Notification Bell Positioning
✅ `NotificationBell.tsx` — Uses `createPortal` to render dropdown  
✅ Positioned anchored to bell icon in sidebar header

---

### 10. Dashboard Pagination Dots
✅ `GridDashboard.tsx` — Multi-page dashboard with pagination dots (592 lines)  
✅ Dots positioned at bottom center with glass container  
✅ Keyboard navigation with INPUT/TEXTAREA guard  
✅ `framer-motion` page transitions

---

## What Needs Fixing

**3 user-facing "Flux" text references remain:**

1. `src/pages/Auth.tsx` line 136 — subtitle text under the auth form header  
2. `src/components/council/CouncilBoardroom.tsx` lines 1266, 1458, 1490 — PDF export and share text content  
3. `supabase/functions/notion-export/index.ts` line 120 — Notion export credit text

## Plan

Fix these 3 remaining "Flux" → "Dashiii" text replacements in:
1. `src/pages/Auth.tsx` — 1 change
2. `src/components/council/CouncilBoardroom.tsx` — 3 changes  
3. `supabase/functions/notion-export/index.ts` — 1 change
